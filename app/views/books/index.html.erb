<h2>New book</h2>
<%= render 'books/book', book: @book %>

<tbody>
          <% @books.each do |book| %>
            <tr>
              <td><%= link_to book.title, book_path(book.id) %></td>
              <td><%= book.opinion %></td>
            </tr>
          <% end %>
        </tbody>